{% extends 'layout.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('editSeason') }}
{% endblock %}

{% block title %}Edit Season{% endblock %}

{% block body %}
    <div class="container">
        <h1>Edit season</h1>
        <h2>{{ series.title }} - season {{ season.number }}</h2>
        {{ include('season/_form.html.twig') }}
    </div>
{% endblock %}
