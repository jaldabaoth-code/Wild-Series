{% extends 'base.html.twig' %}
{% block title %}All series in category
{% endblock %}

{% block body %}
    <h1>{{ actor.name }}</h1>
    {% if actor.poster %}
        <img src="/uploads/{{ actor.poster }}" alt="actor" class="col-2 mb-4">
    {% endif %}
    <p>A joué dans: </p>
    <ul>
        {% for oneSeries in actor.series %}
            <li><a href="{{ path('series_show', {slug: oneSeries.slug}) }}">{{ oneSeries.title }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}