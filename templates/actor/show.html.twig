{% extends 'base.html.twig' %}
{% block title %}All series in category
{% endblock %}

{% block body %}
    <h1>{{ actor.name }}</h1>
    {% if actor.poster %}
        <img src="/uploads/{{ actor.poster }}" alt="actor" class="col-2 mb-4">
    {% endif %}
    <p>A joué dans: </p>
    <ul>
        {% for serie in actor.series %}
            <li><a href="{{ path('serie_show', {slug: serie.slug}) }}">{{ serie.title }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}